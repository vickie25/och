/**
 * Dashboard Client Component - OCH Brand Styled
 * Main dashboard wrapper (if needed for shared layout)
 */

'use client';

export default function DashboardClient() {
  // This component can be used for shared dashboard layout
  // Currently, each role has its own dashboard page
  return null;
}
